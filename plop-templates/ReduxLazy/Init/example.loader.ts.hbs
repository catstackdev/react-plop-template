import type { LazyReducerConfig } from '../../store/types';

export const exampleLazyConfig: LazyReducerConfig = {
  key: 'example',
  reducerPath: './example.slice',
  chunkName: 'reducer-example',
  preload: false,
  dependencies: [],
};