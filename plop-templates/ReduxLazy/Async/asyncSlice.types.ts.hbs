export interface {{pascalCase name}}Item {
  id: string;
  name: string;
  description: string;
  createdAt: string;
  updatedAt: string;
}

export interface {{pascalCase name}}State {
  items: {{pascalCase name}}Item[];
  loading: boolean;
  error: string | null;
  selectedId: string | null;
  lastFetch: string | null;
}

export interface Create{{pascalCase name}}Payload {
  name: string;
  description: string;
}

export interface Update{{pascalCase name}}Payload {
  id: string;
  name?: string;
  description?: string;
}

// Async thunk return types
export interface {{pascalCase name}}AsyncResult {
  data: {{pascalCase name}}Item | {{pascalCase name}}Item[];
  message?: string;
}

export interface {{pascalCase name}}AsyncError {
  message: string;
  code?: number;
  details?: any;
}

// Lazy loading configuration
export interface {{pascalCase name}}LazyConfig {
  key: string;
  reducerPath: string;
  chunkName: string;
  preload: boolean;
  dependencies: string[];
}