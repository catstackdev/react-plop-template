/**
 * {{pascalCase name}} constants
 * Generated by Plop - customize as needed
 */

// String constants
export const {{constantCase name}}_STRINGS = {
  EXAMPLE_MESSAGE: 'Example message',
  ERROR_MESSAGE: 'Something went wrong',
  SUCCESS_MESSAGE: 'Operation completed successfully',
} as const;

// Numeric constants  
export const {{constantCase name}}_NUMBERS = {
  DEFAULT_TIMEOUT: 5000,
  MAX_RETRIES: 3,
  DEFAULT_PAGE_SIZE: 10,
} as const;

// Configuration constants
export const {{constantCase name}}_CONFIG = {
  API_VERSION: 'v1',
  CACHE_TTL: 300000, // 5 minutes
  MAX_FILE_SIZE: 5 * 1024 * 1024, // 5MB
} as const;

// Status/State constants
export enum {{pascalCase name}}Status {
  IDLE = 'idle',
  LOADING = 'loading',
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum {{pascalCase name}}Type {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  WARNING = 'warning',
  DANGER = 'danger',
}

// Regex patterns
export const {{constantCase name}}_PATTERNS = {
  EMAIL: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
  PHONE: /^\+?[\d\s-()]+$/,
  URL: /^https?:\/\/.+/,
} as const;

// Export types for type safety
export type {{pascalCase name}}StatusType = keyof typeof {{pascalCase name}}Status;
export type {{pascalCase name}}TypeType = keyof typeof {{pascalCase name}}Type;